name: Podcast Generator
author: STIT Local
description: Generate a podcast RSS feed

runs:
  using: docker
  image: Dockerfile

branding:
  icon: git-branch
  color: red

inputs:
  email:
    description: Email address
    default: ${{ github.actor }}@localhost

  name:
    description: Name
    required: true
    default: GitHub Actions